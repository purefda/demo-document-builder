import { SubmissionChecklist } from '@/components/submission-checklist';

export default function SubmissionChecklistPage() {
  return <SubmissionChecklist />;
}

export const metadata = {
  title: 'Submission Checklist | Pure Global Document AI',
  description: 'Manage your document submission checklist',
}; 