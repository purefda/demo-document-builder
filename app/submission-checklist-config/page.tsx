import { SubmissionChecklistConfig } from '@/components/submission-checklist-config';

export default function SubmissionChecklistConfigPage() {
  return <SubmissionChecklistConfig />;
}

export const metadata = {
  title: 'Submission Checklist Config Manager | Pure Global Document AI',
  description: 'Manage your submission checklist configurations',
}; 